"use strict"; //Treats all the code as newer version of js
console.log(3 + 3);

let name = "chetan";
let age = 22;
let male = true;

// number
// bigint
// string
// boolean
// null => standalone value (is a repersentation of empty value)
// undefined => declares that a variable is created but is not defined as of now
// symbol => unique

// object

console.log(typeof age); // number
console.log(typeof null); // object
console.log(typeof undefined); // undefined
